# AI 工作手册（停车场业务知识库 2.0）

> 通用机制（写作规范、归档脚本、跨库检索）将上移到脚手架层。
> **在脚手架 v2 落地前，本文是本库的完整规则书**，同时承载本业务线特有约定。

---

# 〇、先确认身份（决定你能不能改文件）

| 判断依据 | 身份 | 权限 |
|---|---|---|
| `原始素材/` 里**有画板文件** | **维护者** | 可按第三、四节写入知识库 |
| `原始素材/` 里**只有 README** | **使用者** | **只读**。可生成产出到 `产出/`，**不得修改** `业务认知/` `设计视角/` `版本迭代/` `待确认.md` |

**使用者模式下发现知识有误**：明确告知用户"这需要维护者更新"，说清是哪个文件哪一句、依据是什么，**不要直接改**。
本手册第三节（写什么）、第四节（归档流程）**仅维护者适用**，使用者可跳过。

---

# 一、先读什么

**任何任务，第一个动作都是读 `索引.md`。** 它用几 KB 交代"有什么、去哪找"。

**不要通读全库。** 不要因为"不确定"就把 `业务认知/` 全部打开。

## 定位的降级链（路由表没命中时按顺序往下走）

1. **`索引.md` 路由表命中** → 只开那 1–2 个文件。注意路由表的「不用读」列，它是明确的劝退。
2. **没命中，但能判断属于哪个业务主题** → 读 `业务认知/00_总览.md` 分诊，再进对应主题。
3. **还不确定** → **用 rg 检索，不要通读**。搜什么：
   - 各文件头部 front-matter 的 `能回答:` 字段（这是逐文件的内容说明，比任何汇总都新）
   - 产品术语、页面名、实体名、需求号（OR/DR/DDR/DOR…）
   - 命中后只开命中的文件，**别顺手把同目录其他文件也打开**
4. **检索仍无果** → 直接说"知识库里没有这部分"，并指出最接近的相关文件 + 可能的原因（该主题未提取 / 属于素材缺口 / 见 `待确认.md`）。**不许推理补齐、不许当作已有能力陈述。**

> 三种场景包（第二节）之外的任务，走这条降级链自行组合。
> 每个文件"讲什么、不讲什么"都写在它自己的 front-matter 里，以那里为准。

---

# 二、场景包（复合任务的读取套餐）

单个问题按 `索引.md` 路由表走。以下三类**复合任务**按套餐走：

## 【场景 A】业务讲解 / 新人交接 / 生成讲解 HTML

```
业务认知/00_总览.md          先建立心智模型与四个全局开关
  → 业务认知/<相关主题>.md    按讲解范围取 1–3 个
  → 设计视角/角色与旅程.md    补"谁在什么场景下用"
  → 产出落到 产出/
```
- 低频任务，允许多读几个文件。
- 默认产出**纯文字**。确需配图时由维护者从 `原始素材/` 现取（素材不入 git，使用者手上没有）。
- ⚠ 产出**不长期维护、不入 git**，头部注明用途/日期/来源。

## 【场景 B】找设计稿

```
页面地图.md   ← 先读这一个定位
版本迭代/vX.Y.md  ← 需要画板名时再开对应版本
```

### ★ 输出必须按「设计文件」聚合，不要按页面分块

素材的真实结构是两层：

```
设计文件（一个版本 × 一个端 = 一个文件，唯一地址）
  └── 画板（PNG 名，在文件内按名字找）
```

**用户的动作是「打开一个设计文件，在里面找几个画板」。**
所以哪怕问题涉及三个页面，只要它们落在同一个设计文件里，**就合并成一条**：先说开哪个设计文件，再列出该文件内要找的画板清单。

❌ 错误：按页面分三大块，每块各写一遍"去哪找"
✅ 正确：「打开 `HCP CS V3.1.1_车辆与停车管理`，在里面找这 4 张画板：…」

跨版本或跨端时才分块，且**按设计文件分**，不按页面分。

### 其他要求
- **必须先确认平台（BS / CS）**，同一业务两端各有一个设计文件，同名画板内容不同。
- **必须说清楚每张画板是不是完整的**：
  - 有基线 → "完整基线是 X 版的《画板名》，之后还有 N 张局部改动画板要叠加看"。
  - 标 ⚠ 无基线 → **明确告知"该页面没有整页稿"**，只能按增量拼或去实际环境看。
- 不要只丢一条"最近变更"就说这是最新稿——那大概率只是一个下拉框。
- ⚠ **17/24 的页面无整页基线，这是既有状态不是缺陷**，不要每次都建议"去补稿"。
- ⚠ **给的是检索线索不是图**：必须带上 **设计文件名 + 画板名（+需求号）**，这是用户去设计软件里检索的关键词。使用者本地没有 `原始素材/`。
- ⚠ **一张画板可能承载多条变更**（如 v3.1.1 BS 的「功能及数据显示优化」一张覆盖 4 条），同一画板名重复出现时合并说明，别列成四张不同的图。

## 【场景 C】设计咨询 / 出方案 / 给专业建议

```
必读：
  业务认知/<主题>.md              规则本质 + 边界与异常
  版本迭代/演进脉络.md            这条线的来龙去脉、留下的包袱
  待确认.md                       别把存疑当事实
再读：
  设计视角/设计范式.md            有没有既有解法可沿用
  设计视角/体验缺口.md            已知问题与机会点
```
- ⚠ **事实与建议必须分开说**：哪些是已有能力、哪些是你的提议。
- ⚠ 命中 `待确认.md` 条目**必须点明存疑**，不许靠推理补齐。
- ⚠ 涉及名单 / 有效期的方案，必问一句：**"这对只有 ANPR License 的客户有什么影响？"**（见演进脉络主线三）

---

# 三、写什么、不写什么

## `业务认知/<主题>.md` = 概念，不是需求清单

只写**不随版本变的业务知识**。推荐顺序：
1. 一句话定位 → 2. 核心流程（**配置链路** + **应用链路**，很多模块只讲了应用、漏了配置）→ 3. 核心概念与实体关系 → 4. **规则的本质**（为什么这么定，而非取值）→ 5. 心智模型口诀 → 6. **边界与异常** → 7. FAQ

**不写**：版本号、单条需求、提示文案、按钮二次确认、控件默认值、字段级 UI。

> 判断标准：读完能回答"**是什么 / 为什么 / 怎么跑**"，而不是"这个版本改了哪些点"。
> 若一段话带了版本号、或在描述某个弹窗/提示的具体行为，它多半该在 `版本迭代/`。

**例外**：`边界与异常` 一节允许标注出处版本，因为"这条约束从哪版开始"本身是有用信息。

## `版本迭代/vX.Y.md` = 需求与版本细节

每条固定这几个字段：

```
### <变更点标题>
`类型` · `范围` · <业务主题> · <平台> · <需求号（若有）>
- **页面**：菜单路径
- **改了什么**：
- **为什么**：
- **注意**：
- **素材**：文件名
```

| 字段 | 取值 |
|---|---|
| 类型 | 🆕新增功能 / ✏️优化 / 🔁整合重构 / 🌐定制转基线 |
| **范围** | **整页** / **区块** / **单控件**（★ 决定稿子完不完整，必填） |
| 平台 | BS / CS |

## `版本迭代/演进脉络.md` = 判断，不是汇总

只写**为什么变、留下什么包袱、现在该怎么用**。逐条改动指向版本文件。**手写，不可生成。**

## `设计视角/` = 设计侧知识

与业务认知的分工：同一个"包期"，业务认知讲**规则为什么这么定**，设计视角讲**在界面上怎么表达、有哪些既有范式**。
`体验缺口.md` 是**观点层**，会被推翻，不要和事实层混写。

## `待确认.md` = 拿不准的一律登记，不写进业务认知

---

# 四、归档流程

1. **人**：把新版本的画板 / 实况截图放进 `原始素材/`，告知 AI 版本号。
2. **AI**：读 `索引.md` 归档台账 → **只对未提取的图**切片、读图。
3. **AI**：
   - 追加条目到 `版本迭代/v<新版本>.md`，**页面与范围两个字段必填**；
   - **仅当业务概念真的变了**（新增实体、规则本质改变）才动 `业务认知/`；
   - 判断本次改动是否属于 `演进脉络.md` 某条主线，是则追加一段；
   - 拿不准的登记到 `待确认.md`；
   - 更新 `版本迭代/README.md` 时间线（**表格，每版一行，禁止叙述**）；
   - 重生 `索引.md` 前两表 + `页面地图.md` 增量列；
   - 在 `索引.md` 顶部写"最近归档 + 本次三个最重要发现"。
4. **AI**：输出**评审清单**给人——我改了什么、哪里存疑、哪些没读。
5. **人**：评审 → commit。

> 关键判断：**大多数版本迭代只该动 `版本迭代/`，不动 `业务认知/`。**
> 若 AI 往业务认知里塞了版本号或弹窗细节，打回。

---

# 五、省 token 铁律

1. **图只读一次**：台账标 ✅ 的画板不再读图；仅设计变更时重读。
2. **下游只读文本**：答问、出产出、更新，一律基于 `业务认知/` + `版本迭代/`，**不重扫全盘、不重读图片**。
3. **开工先读 `索引.md`**，用几 KB 重建上下文。
4. **产出是快照**：不在 `产出/` 手工长期维护，需要就重生。合并版/完整版属按需产出，**不放 `业务认知/`**。
5. **导航文件必须薄**：`索引.md`、`版本迭代/README.md`、`00_总览.md` 一旦写厚就不再是导航。写不下的进明细文件。

---

# 六、本业务线特有约定

## ★ 端（本库声明；脚手架不预设端的数量与名称）

> **端 = 有独立设计稿的界面载体。** 不是"业务上涉及几种硬件"。
> 被配置的设备界面（显示屏、缴费机、寻车终端）**不算端** —— 它们的稿子画在配置端里。
> 哪天某个设备自己出了成套设计稿，才把它升格为端，并在本表加一行。

### 本库现有的端

| ID | 全称 | 定位 | 谁在用 |
|---|---|---|---|
| **BS** | Web 管理端（Browser/Server） | **配置侧**：建场、配规则、配设备、查报表 | 实施工程师、物业管理员、运维 |
| **CS** | 控制客户端（Client/Server），含岗亭 | **值守侧**：现场放行、现场收款、交班 | 岗亭值守员、中心收费员 |

### 不是端的（被配置对象）

寻车终端 / 寻车 APP、缴费机、出入口显示屏、诱导屏、车位屏 —— 它们的界面由 BS 配置，**设计稿都在 BS 的配置页里**（如 v3.2 的 LOGO 拖拽器、键盘编辑器画的是 BS 的编辑界面）。

### 端相关的三条规则

1. **设计文件 = 一个版本 × 一个端。** 端增加时，同一版本就有多个设计文件，在版本文件头部逐个声明。
2. **页面 ID 必须带端**：`<端> > <菜单路径>`，如 `CS > 收费概览`。同一业务在两端是两个页面，不是一个。
3. **★ 一条变更条目只能属于一个端。** 跨端需求**拆成每端一条**（稿子本来就是分开的），用**相同标题 + 相同需求号**关联，各自指向自己端的画板。
   - 例：v3.1.1「包期放行逻辑优化」BS 与 CS 各一条，画板同名但内容不同。
   - ❌ 不要写成一条条目挂多个端、指向多个文件。

## 版本
- 目录命名有 `V3.1` 与 `V3.1.1` 两套，且 `V3.1` 目录里部分画板实际标注为收费定稿 2026-03。
  **版本归属以画板内标注 / 需求号为准**，提取时校正。
- v2.4 是**白皮书基线**，不是迭代版本。与 3.x 冲突时**以 3.x 为准**，并登记到 `待确认.md`。

## 素材（两层粒度，别混）

```
设计文件 = 一个版本 × 一个端，唯一地址   例：HCP CS V3.1.1_车辆与停车管理
  └── 画板 = 文件内的一张 PNG            例：收费概览-设计定稿-20260312
```

- 版本文件**头部**声明本版有哪几个设计文件（BS/CS 各一个）。
- 每条变更的 `画板` 字段**只写画板名**，不写目录、不写"同上"。
- 一张画板承载多条变更时，多条重复引用同一个画板名，并注明是其中哪一部分。
- **只增不改**，供溯源。
- 冲突规则：**业务概念以实况截图为准，版本变更以画板 / 需求号为准。**
- 设计稿文件名保留**需求号（OR/DR/DDR/DOR…）**，便于与需求单对应。
- v2.5 曾有两个目录（PDF 版与 PNG 版），**读图一律用 PNG 版**。

## 命名
- 业务主题对齐停车场菜单；**业务认知 MD 按主题命名，不按版本**。
- ⚠ 全库**禁用「模块」一词**（歧义：研发指功能闭环、设计师指三级页面）。统一用：
  - **业务主题** = 功能闭环（放行规则、收费管理）
  - **页面** = 界面位置（停车场详情 > 车道配置）
- 屏类型、"余位统计→车位预留"等历史命名变更见 `业务认知/术语与实体.md`，看老稿需换算。
