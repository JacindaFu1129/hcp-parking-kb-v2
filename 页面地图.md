# 页面地图（设计师入口）

> **你要找"某个页面最新的设计稿在哪"，读这一个文件就够。**
> 数据来源：`版本迭代/*.md` 各条目的 `页面` + `范围` 字段，按页面重排。本文没有自己的内容，是索引不是副本。
>
> ## 怎么读这张表
> - **完整基线** = 该页面最近一次 `范围 = 整页` 的稿子。打开它能看到整页。
> - **基线后的增量** = 基线之后的所有改动，**只画了改动部分**，要叠在基线上看。
> - ⚠ **无基线** = 这个页面从来没有过整页稿，只有一轮轮局部优化。**别指望找到一张全的**，只能按增量拼，或去实际环境里看。
> - 该页面有没有实况截图等其他素材，查 `索引.md` 归档台账。
> - ⚠ **本表给的是指针，不是图。** 拿到 版本 + 素材文件名 + 需求号后，自己去设计软件里检索。
>
> 【菜单树区｜手工维护，菜单结构变了才动】
> 【基线与增量列｜脚本每次归档刷新】
> ⚠ 首轮迁移说明：`范围` 字段由**变更描述文字推断**，未逐张开图核对。下次归档时校正。

---

## BS · Web 管理端

### 车辆概览

| 页面 | 完整基线 | 基线后的增量 |
|---|---|---|
| 车辆概览（首页） | ✅ **v3.1**《车辆概览展示优化》整页 | — |

> v3.0《停车场概览一键刷新》早于基线，已被 v3.1 整页稿覆盖。

### 停车场管理

| 页面 | 完整基线 | 基线后的增量 |
|---|---|---|
| 停车场详情 > 出入口 > **车道配置** | ✅ **v3.1.1**《车道配置布局优化》整页重构 | v3.2 车道控制模式细化（单控件） |
| 停车场详情 > **放行规则** | ⚠ **无基线** | v2.5 离线放行·名单车位管理（区块）<br>v3.0 引入放行判断方式（区块）<br>v3.0 固定车位统计重构（区块）<br>v3.1.1 删单项条件认证（区块）<br>v3.1.1 复制节假日失败详情（单控件）<br>v3.2 超时不自动放行（单控件） |
| 停车场详情 > **配置显示屏** | 🔶 v3.0《出入口 LCD 信息配置统一》整合稿（区块，但覆盖了整块配置） | v3.1.1 余位为0显示统一（单控件） |
| 停车场详情 > 基础配置 > 收费小票配置 | ✅ **v3.1** 新增（区块，页面即该区块） | — |
| 停车场详情 > 出入口 > 对讲柱 | ⚠ **无基线** | v3.2 兼容 TMT101/102（**未读图**） |

### 收费

| 页面 | 完整基线 | 基线后的增量 |
|---|---|---|
| 收费规则 | ⚠ **无基线** | v3.1 一户多车计费模式（单控件）<br>v3.2 额外收费规则（区块，新增规则类型）<br>v3.2 放重复进（单控件） |
| 充值管理 | ⚠ **无基线** | v2.5 打印收费凭证（区块）<br>v2.5 有效期按停车场分行 + 四状态（区块）<br>v3.1.1 包期放行逻辑优化（区块） |
| 统计分析 > 收支统计 | ⚠ **无基线** | v3.1 支付来源与方式分析（区块） |

### 车辆与名单

| 页面 | 完整基线 | 基线后的增量 |
|---|---|---|
| 车辆 > **车辆属性设置** | ✅ **v2.5**《阿联酋定制转基线（车辆侧）》整页新增菜单 | — |
| 车辆管理列表 | ⚠ **无基线** | v2.5 有效期列分行（区块）<br>v2.5 国家地区/Category 列（单控件）<br>v3.0 车主 ID（单控件）<br>v3.1.1 IC/UHF 卡区分（单控件）<br>v3.1.1 批量权限编辑（单控件） |
| 车辆 > 名单管理 > **名单下发** | ⚠ **无基线** | v2.5 名单下发（ANPR 分支，区块）<br>v2.5 名单下发（出入口分支，区块）<br>v2.5 下发结果与失败详情（区块） |
| 车辆 > 名单管理 > 访客车名单 | ⚠ **无基线** | v3.1 手动删除访客车（单控件）<br>v3.1 展示停车场权限（单控件） |
| 人员 > 车辆通行 ／ 车辆 > 停车场通行 | ⚠ **无基线** | v3.0 停车场选择改资源树（单控件）<br>v3.1 免费场权限理顺（区块）<br>v3.1.1 **收费场改为只读 + 跳转充值**（区块）<br>v3.1.1 未配权限提示（单控件） |

### 停车诱导

| 页面 | 完整基线 | 基线后的增量 |
|---|---|---|
| 停车诱导配置 > **楼层向导（四步）** | ⚠ **无基线**（配置链路以 v3.1.5 实况为准，见 `索引.md` 台账） | v2.5 显示车位号（单控件）<br>v2.5 自助设备朝向（单控件）<br>v2.5 批量配置车位（区块）<br>v3.1 车位屏绑定（区块）<br>v3.1 停放规则默认选型（单控件）<br>v3.1 矢量图替换保留（单控件）<br>v3.2 分区颜色（单控件） |
| 停车诱导配置 > 车位类型管理 | ⚠ **无基线** | v3.0 自定义类型与颜色（区块）<br>v3.1.1 取色器内嵌 pop（单控件） |
| 停车诱导配置 > 显示屏配置 | 🔶 v2.5《多种诱导屏合并》三分类归一（区块，重定义了屏类型） | v2.5 跨停车场余位统计（单控件）<br>v2.5 TVB200 查看屏状态（区块）<br>v3.0 室外引导屏 FULL（单控件）<br>v3.1.1 余位为0显示统一（单控件） |
| 停车诱导配置 > 寻车客户端配置 | ⚠ **无基线** | v3.2 LOGO 位置自定义（区块）<br>v3.2 自定义键盘编辑器（区块） |
| 车位监控 / 停车场地图 | ⚠ **无基线** | v3.0 矢量图看车位编号（单控件）<br>v3.1 地磁状态（区块） |

### 检索与报表

| 页面 | 完整基线 | 基线后的增量 |
|---|---|---|
| 统计分析 > **停车场运营分析** | ✅ **v2.5**《与 BI 保持统一》整页 | — |
| 检索 > 出入口车辆检索 | ⚠ **无基线** | v2.5 抓拍图命名排序（区块）<br>v2.5 SIRA 筛选与列（区块）<br>v3.1.1 补齐车道/放行备注列（单控件） |
| 检索 > 车场剩余车辆检索 | 🔶 v3.0《纯诱导场景可查场内车辆》（区块，新增检索形态） | v3.0 批量删除（单控件）<br>v3.2 SIRA 多字段（区块） |
| 检索 > 缴费记录检索 | ⚠ **无基线** | v3.1 收费方式/来源字段（单控件）<br>v3.1.1 按收费规则过滤（单控件） |
| 过车监控 | ⚠ **无基线** | v2.5 车载 ANPR + 地理位置 Tab（区块）<br>v3.0 车辆分析上限提示（单控件） |
| ANPR 报表 | ⚠ **无基线** | v3.1.1 按速度过滤（单控件） |

### 设备与运维

| 页面 | 完整基线 | 基线后的增量 |
|---|---|---|
| 设备管理 > 缴费机 | ✅ **v3.1** 缴费机添加与运维（区块，页面即新增） | v3.2 收币将满阈值 + 钱箱状态列（单控件） |
| 系统运维 > 备份 | ⚠ **无基线** | v3.1 备份路径控件降级（单控件） |

---

## CS · 客户端 / 岗亭

| 页面 | 完整基线 | 基线后的增量 |
|---|---|---|
| CS > **收费概览** | ✅ **v3.1.1**《收费概览-设计定稿-20260312》整页新增 | — |
| CS > 出入口监控 | ⚠ **无基线** | v3.1 远端站点停车场（区块）<br>v3.1 SIRA 车牌监控页（区块）<br>v3.1.1 放行文案与登记放行字段（区块） |
| CS > 出入口监控 > 出场收费 | ⚠ **无基线** | v3.1.1 多规则明细 + 超免费时长重算（区块） |
| CS > 交班 / 结束工作 | ⚠ **无基线** | v3.1.1 二次确认（单控件） |

---

## 体检提示

| 告警 | 页面 |
|---|---|
| 增量 ≥3 且无基线 | **停车场详情 > 放行规则**（6 条）<br>**停车诱导楼层向导**（7 条）<br>**车辆管理列表**（5 条）<br>**人员/车辆停车场通行**（4 条） |

> **24 个页面中 17 个无整页基线。** 这是本库的既有状态，不是待办 —— 历史上多数需求就是局部优化，本来就没有整页稿。
> 它的实际含义是：**回答"这页最新稿在哪"时必须说清楚给出的是局部还是整页**，而不是"该去补一批稿"。
