# 停车场业务知识库 · AI 入口

**完整规则见 [AGENTS.md](AGENTS.md)，请先读它。**

三条铁律先记住：

1. **第一个动作是读 `索引.md`**，按路由表定位到 1–2 个文件。**不要通读全库。**
2. **先判断身份**：`原始素材/` 里只有 README → 你是**使用者模式**，**只读**，不得修改 `业务认知/` `设计视角/` `版本迭代/` `待确认.md`；发现知识有误只告知用户"需要维护者更新"，说清哪个文件哪一句。
3. **找不到就说找不到**，指出最接近的文件和可能原因，**不许推理补齐、不许把推断当已有能力陈述**。
